spring:
  application:
    name: tradebyte-challenge
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:todo_list_repo
    driverClassName: org.h2.Driver
    username: sa
    # password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    # hibernate:
    #      ddl-auto: none
